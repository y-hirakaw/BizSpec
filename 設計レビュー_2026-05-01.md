# BizSpec 設計レビュー（2026-05-01）

4 つの観点（コードアーキテクチャ／CLI・UX／データモデル／AI エージェント統合）から並列レビューした結果と改善優先順位をまとめる。機能の有無ではなく **設計品質** を評価対象とした。

---

## 1. コードアーキテクチャ

### 強み
- コマンド単位のモジュール分割が一貫（`<verb>_cmd.py` × `run_<verb>(args) -> int`）。新コマンド追加が予測可能
- `cli.py` (105行) はパーサ定義とディスパッチのみで CLI 層が薄い
- `validate.py` の `VError` dataclass による構造化エラー設計、`_check_file` / `_check_process` の分離

### 懸念
- **共有 IO レイヤー不在**: `_load_units` 系が list/viz/search/renumber/rename に類似実装で散在。`_PREFIX_RE` / `_write_yaml` / `_find_unit_file` も重複
- **YAML パース失敗を黙殺**: `try: yaml.safe_load... except Exception: pass` が 5 箇所以上に散在。壊れた YAML が list/viz/search で透明に消えるバグの温床
- **`viz_cmd.py` が 1985 行に肥大化**: HTML/CSS/JS テンプレートを Python 文字列リテラルに埋め込み、`__PLACEHOLDER__` 文字列置換でデータを差し込む構造
- **エラーハンドリング戦略が3種類混在**: (a) `print(..., file=sys.stderr)` + return int、(b) 構造化 `VError`、(c) 黙殺
- **逆向き依存**: `rename_cmd.py` が `from bizspec.validate import _check_process` を関数内 import（循環回避のローカルインポート）
- **型ヒント・命名の網羅性低い**: `args` は型注釈なし、unit データの型は dict any。`list_cmd.py` に isinstance チェック地獄

### 改善提案
1. **【高】共有レイヤー `bizspec/core/` の抽出**: `loader.py`（load_units / iter_processes 等）、`model.py`（Unit TypedDict）、`errors.py`（BizSpecError + VError）。各 `_cmd.py` は 30〜50% 削減可能、関数内 import も解消
2. **【高】`viz_cmd.py` の分割**: `viz/layout.py` / `viz/builder.py` / `viz/templates/{process,index}.html`（外部ファイル化、`importlib.resources` で読み込み）/ `viz/cmd.py`
3. **【中】エラーハンドリング統一**: `BizSpecError(message, hint=None)` を定義し各 `run_*` は raise、`cli.py` で集約 catch → exit code 変換。`except Exception: pass` は `except yaml.YAMLError as e: warnings.append(...)` に変更

---

## 2. CLI / UX 設計

### 強み
- 動詞ベース命名（init/new/list/rm/rename/search/validate）で Unix 慣習に沿う
- 位置引数階層が「広→狭」で揃う（`bizspec <verb> <process> [<unit>]`）
- 破壊系コマンドが `--dry-run` + 自動 validate + フロー分断検知で安全策が複層的

### 懸念
- **位置引数名の不一致**: `new <unit>` / `rm <unit_name>` / `rename <old_name>` `<new_name>` とバラバラ。シェルスクリプトで包む際にハマる
- **`--format` が `list` だけ**: `validate` / `search` / `viz` は人間向けテキストのみ。CI から JSON で読みたい時に grep/awk で組み立てる羽目に
- **確認プロンプト不在**: `rm` / `rename` が無言で本番ファイルを更新。`--dry-run` を知らないユーザーは初見で破壊
- **stdin/stdout 規約なし**: `bizspec list --format json | bizspec something` のようなパイプ運用が不可能、`-` を stdin/stdout に解釈する慣習もなし
- **発見可能性とエイリアス不在**: argparse デフォルト help のみで例示ゼロ、シェル補完スクリプト未同梱、`--version` 未提供、typo サジェスト無し

### 改善提案
1. **【高】位置引数名の統一と `-y/--yes` 追加**: 全サブコマンドで unit を指す位置引数を `unit` に統一。`rm`/`rename`/`renumber` に TTY 検知でデフォルト確認プロンプト + `-y/--yes` で抑止、非対話環境では従来通り即実行
2. **【中】`--format {text,json,yaml}` を validate / search / viz manifest に拡張**: AI/CI 連携の中核に位置付け（プロダクトの「AI-Native」価値と整合）。終了コードも `validate エラー=1 / I/O=2 / usage=64` のように分ける
3. **【中】補完スクリプト同梱と `--version`**: `argcomplete` でプロセス名・unit 名（ファイルから動的列挙）の補完。日本語 unit 名運用で効果大

---

## 3. データモデル（BizSpec YAML）

### 強み
- IPO（io.in/run/out）+ executor の二軸が業務分解の自然なメンタルモデルに沿う
- Optional の段階拡張が後方互換を保つ（v1 で effort/automation/status 追加しても必須フィールド維持）
- 双方向リンクの整合性検証（`link.up/down` クロスファイル対称性、`depends_on` 存在確認）

### 懸念
- **`job` と `io.run` が実例で同義反復**: `OpenAPI定義作成.yaml` の `job`（"OpenAPI YAML を記述／Lint で検証"）と `io.run`（"OpenAPI YAML/JSON を作成／Lint で検証"）はほぼ同一文。ルーブリックがあっても書き分けコストが高く DRY 違反を誘発
- **3つのリンク概念が重複**: `link.up/down`（実行順序）／`depends_on`（他プロセス依存）／`precondition`（開始前提）。実例では `precondition` 0 ファイル、`depends_on` 1 ファイルのみで `link` だけで運用されている
- **業務記述として表現できない概念が多い**:
  - 失敗時挙動・ロールバック条件（`本番デプロイ.yaml` で "ロールバック手順を確認" と `rule` 文字列に潜伏）
  - ロール／責任者（`executor.type: manual` だけでは "誰が" が表現できない）
  - トリガー（`インシデント検知.yaml` のアラート受信は entry 条件だが `io.in` で表現）
  - SLA／品質指標（`Sev1 5 分以内` が `rule` の自然言語にしか無い）
  - 成果物の検証基準（acceptance criteria）
- **`core: undetermined` がスキーマで表現できない**: CLAUDE.md と要件仕様 §4 では許可、`validate.py:97-99` は `bool` のみ許可（仕様乖離）
- **`phase` の自由記述が分類不能**: `インシデント検知.yaml:3` は `ops` を使用（CLAUDE.md の例示 spec/dev/test/release に含まれない）

### 改善提案
1. **【高】リンク概念を 2 軸に整理**: `link.up/down` は「同一プロセス内の順序」専用、`depends_on` は「プロセス境界を越える成果物依存」専用に切り分け、`precondition` は廃止または `entry_condition`（boolean 式 / 文字列）として再設計
2. **【高】業務記述に必要な軽量フィールドを v2 で追加**:
   - `trigger`: イベント駆動の起動条件
   - `on_failure`: 失敗時の戻し先 unit／回復手順
   - `role`: 実行責任ロール（`executor.type: manual` と直交）
   - `sla`: `{response_time, threshold}` の構造化
   - `acceptance`: 出力受入基準のリスト
3. **【中】`io.run` の廃止または `job` への統合**: 重複が常態化しているため `job` を「アクション手順」に一本化、`io` は `in/out` のみのデータ契約に絞る。`io.in/out` を `{name, type, source}` の構造化形式に拡張すればデータフロー可視化と検証が強化される。併せて `core: undetermined` の許容と `phase` の推奨語彙（warn レベル）を追加

---

## 4. AI エージェント統合・Skills 設計

### 強み
- `bizspec-refine`（縦の分解：IPO 再帰）と `bizspec-refactor`（横断分析：7軸）の責務が直交し、`refactor` 末尾で `refine` への引き継ぎ動線も明示
- Human-in-the-loop が具体的：`core` スクリーニング表で「迷う場合 → ユーザー確認」を固定、「AIが独断で `core` を確定させてはいけない」と禁止句あり
- フィールド責務ルーブリック（job/rule/io.run の書き分け、品詞ヒューリスティクス）が SKILL.md に同梱され、プロンプト単体で自己完結

### 懸念
- **`.claude/skills/` と `src/bizspec/skills/` が完全重複**: 正本ルール未文書化。`bizspec init` が `rmtree` で既存を無警告削除（init_cmd.py:46）、ユーザー編集が破棄されるリスク
- **`refactor` と `refine` の方針矛盾**: `refine` は CLI 経由を推奨（行120-121）だが、`refactor` は「YAML を直接更新」と書いている（行163-166）。削除/リネーム/マージは `bizspec rm`/`rename` を使うべきで、直書きは link 整合性破壊のリスク
- **拡張フィールドが `refine` の出力テンプレに無い**: `effort`/`automation`/`status`/`precondition`/`execution.parallel_with`/`depends_on` への言及欠落 → AI 生成 YAML から恒常的に欠落 → `refactor` のヒートマップ前提（`effort` を一覧）が崩れる
- **再帰分解の停止条件不在**: 深さ上限・unit 数上限の言及なし。極端な業務での暴走リスク
- **CLI 利用の動線が暗黙**: `refactor` で `bizspec list --refactor` を使う流れは記載されているが、他のスキル（refine 含む）から `bizspec search` / `validate` / `new` を呼ぶ動線が散発的

### 改善提案
1. **【高】`refactor` の CLI 誘導を refine と揃える**: `refactor` SKILL の「適用できる変更」を「`bizspec rm` / `bizspec rename` を必ず介して適用する。link 更新は隣接 unit の手動編集が必要な場合のみ Edit を使う」と書き換え。両 skill のポリシーを統一
2. **【高】拡張フィールドを refine の出力フォーマットに追加**: `effort`/`automation`/`status`/`precondition`/`execution.parallel_with`/`depends_on` を「省略可」として追記し、「ユーザーが時間や頻度に言及したら effort を埋める」等の記述ヒューリスティクスを与える。`refactor` の前提が満たされる
3. **【中】同梱 Skills の正本ルールと深さ上限の明文化**: CLAUDE.md に「SKILL.md の正本は `src/bizspec/skills/` 側、`.claude/skills/` 側は `bizspec init` の出力なので編集禁止」と明記。`refine` に「unit 数 15 / 階層 4 段超で分解停止 → ユーザーに統合可否を問う」を追加。新スキル追加時のテンプレ（フロントマター + 役割 + 停止条件 + CLI 誘導 + Human-in-the-loop ポイント）も整備

---

## 横断的に見える優先テーマ

| 優先 | テーマ | 該当箇所 |
|---|---|---|
| **A** | **概念重複の整理** | `job` ↔ `io.run` ／ `link` ↔ `depends_on` ↔ `precondition` ／ `refine` ↔ `refactor` の CLI 利用方針 |
| **B** | **共有レイヤー抽出** | `bizspec/core/{loader,model,errors}.py` で `_load_units` 重複・3種エラー戦略・型不在を一掃 |
| **C** | **構造化出力の拡張** | `--format json` を `validate`/`search`/`viz` まで広げ、AI/CI 連携の中核に位置付け（「AI-Native」と整合） |
| **D** | **Skills の整合と拡張フィールド対応** | refine 出力テンプレに拡張フィールド追加 + refactor の CLI 誘導統一 + 同梱 SKILL.md の正本ルール明文化 |

特に **A の「概念重複」** はコード・CLI・スキーマ・Skills の4レイヤー全部に表面化しており、設計初期に「並べた」概念がそのまま残って整理されていない兆候。v2 を切るタイミングで一気に整える価値が高い。

---

## 推奨着手順序

1. **DAY 1（小）**: `refactor` SKILL の CLI 誘導書き換え／refine 出力テンプレに拡張フィールド追記／同梱 SKILL.md の正本ルール記述
2. **WEEK 1（中）**: `bizspec/core/` 抽出 → 各 `_cmd.py` リファクタ → `except Exception: pass` 撲滅
3. **WEEK 2（中）**: `--format json` を validate/search に追加 → exit code 整理 → 位置引数名の統一
4. **MONTH 1（大）**: BizSpec YAML v2 設計議論 — `io.run` 統合、リンク 2 軸整理、`trigger`/`on_failure`/`role`/`sla`/`acceptance` 追加可否
5. **MONTH 1（大）**: `viz_cmd.py` 分割（`viz/` サブパッケージ + テンプレート外部化）
