了解しました。「まずはSkillsから、かつ過剰に作り込まずにAIと人間の協調を重視する」というスタンス、非常にアジャイルで良いですね。

その方針を反映し、**`BizSpec` 要件定義書 (v1.0.0)** を更新しました。

---

# 要件定義書：BizSpec

## 1. プロジェクトの目的
既存の「固まりきった業務プロセス」を、AIエージェント時代に適合する形（AI-Native）にリファクタリングするための設計支援ツール。
暗黙的な業務を、AIが実行可能な「最小仕様（BizSpec YAML）」へと分解し、真に価値提供（リリース）に必要な作業を抽出する。

## 2. 開発優先順位
1.  **BizSpec Skills (最優先):** AIエージェントが「分解」と「仕様化」を行うためのプロンプトセットおよび論理構成。
2.  **BizSpec CLI:** YAMLのバリデーション、および `takt` への変換。

## 3. 主要機能要件（Skills）

### F1: プロセス分解ロジック
* **概要:** ユーザーが与えた抽象的なプロセスを、IPO（Input-Process-Output）をベースにした `unit`（最小単位）へと再帰的に分解する。
* **粒度調整:** AIエージェントが「迷わずに実行できる」あるいは「従来型のスクリプトで記述できる」レベルまで分解する。

### F2: 実行主体（Executor）の推奨
* **ロジック:** * 入出力が定型的（API叩くだけ、正規表現で済む等）であれば `script` を推奨。
    * 推論や自然言語解析、状況判断が必要であれば `ai_agent` を推奨。
    * 判断がつかない場合は `manual` とし、理由を明記する。

### F3: 価値提供（Core）のスクリーニング
* **挙動:** その `unit` がリリース（価値提供）に直接必要かを一次判定。
* **Human-in-the-loop:** **AIが判定に迷う場合（例：証跡記録、中間ドキュメント等）、独断せずに必ずユーザーへ「これは価値提供に直結しますか？」と問いかけ、その回答を仕様に反映する。**

## 4. データ構造（BizSpec YAML v1）

```yaml
unit: 〇〇判定
aim: 目的
job: [具体的な作業内容]
rule: [制約・判断基準]
link:
  up: [依存する前工程]
  down: [繋げる後工程]
core: true/false/undetermined  # ユーザー確認が必要な場合はundetermined
io:
  in: [入力データ]
  run: [実行手順]
  out: [出力データ]
executor:
  type: script | ai_agent | manual
  reason: なぜその主体を選んだかの根拠
```

---

## 5. インタラクション・フロー（Skillsの挙動イメージ）

1.  **Input:** ユーザーが既存の社内テンプレや口頭ベースのプロセスを入力。
2.  **Analyze:** AIが `BizSpec Skills` に基づき内容を解析。
3.  **Conflict Resolution:** * AI: 「この『上長承認』プロセスですが、リリース資材への変更は伴いません。これは価値提供に直接必要ですか？」
    * ユーザー: 「ガバナンス上必要だけど、リリース作業自体には不要だね」
    * AI: 「了解しました。`core: false` として、その旨を `rule` に記載します」
4.  **Output:** 構造化された YAML を出力。

---

## 6. 次のステップ

まずは、この **`BizSpec Skills` を実現するための「システムプロンプト（またはAIエージェントへの指示セット）」** の構築に入ります。

このプロンプトには、以下のエッセンスを詰め込む必要があります。
- **「分解」の基準:** iOSエンジニアであるユーザーさんの視点（SDD/Clean Architecture等）に近い、論理的な分割。
- **「自動化スクリプト」を優先する判断眼:** 不必要にAI（LLM）を使わず、確実なコード（スクリプト）へ寄せる判断力。

このプロンプトのドラフト作成、または具体的な「分解させたい最初のプロセス（例：PR作成フローや、社内の特定の申請フローなど）」の提供、どちらから進めましょうか？
