# BizSpec 設計メモ（2026-05-08）

YAML レビュー負荷 → 3波の実装 → 規模拡張の議論 → 抜本的な再設計方針までを記録する。

---

## 1. 出発点の痛み

> 「YAML を精査していく作業が苦痛。文章だから結局全部読んでレビューして作っていかないといけない」

レビュー時に YAML ファイル群を順に開いて読む負荷が高い。

エージェントチーム3名で診断した共通結論：
**スキーマ検証は「正しさ」を見るが、「妥当さ」と「変化点」と「要レビュー箇所」は人間の縦読みに丸投げになっている。**

---

## 2. 実装済み（3波）

### 第1波: viz の Review フィルタと件数バッジ

**コミット**: `0897de8 viz: add Review filter and per-process review badges`

- `process.html` / `index.html` に Review トグル
- 判定基準: `status ∈ {draft, review}` ∨ `core = undetermined` ∨ `executor.reason` が空
- index のサイドバー / overview カードに該当件数バッジ
- ノイズ防止: 0件のプロセスでは UI を出さない
- テスト 19件追加

### 第2波: /bizspec-review スキル

**コミット**: `01d98c7 Add /bizspec-review skill and _defaults.yaml field inheritance`

- 7軸の意味整合レビュー（責務混線・core 妥当性・粒度・executor根拠・ライフサイクル・命名・入出力整合）
- 重大度 🔴🟡🟢 付き Markdown レポート出力
- **YAML は書き換えない**（修正は `/bizspec-refine` 引き継ぎ）
- 役割分担: refine=書く / refactor=直す / review=読む

### 第3波: `_defaults.yaml`（プロセス共通の既定値）

- `core/loader.py` に `load_process_defaults` / `apply_defaults`
- viz / list / search / validate でマージ後の実効値を扱う
- 書き込み系（new/rename/rm/renumber）は raw のまま
- 完全後方互換

**ただし実データへの適用は撤回**。理由:
- viz は読む専用ツールであって編集はできない
- unit YAML を編集する瞬間に `_defaults.yaml` との往復が発生し、indirection が増える
- 機能としては残置（将来「この値は絶対変わらない」と確信した時点で個別採用）

---

## 3. 規模拡張の議論で見えたこと

> シナリオ: 100プロセス × 500unit が出来上がったら？

エージェント3名が並行検討した結論：

### 3.1 レバレッジ可視化までの必要十分情報

**現状はほぼ揃っているが、3つだけ足りない：**

| 不足項目 | 何のため |
|---|---|
| `reuse_key`（共通パターンID） | 横断重複の検出 |
| `automation.expected_status` | 改善余地（現在→到達 差分） |
| `effort.headcount` | 100×500 規模の人月精度 |

**レバレッジ式案**:
```
leverage = monthly_cost × manual_share × automation_gap × difficulty_inv
reuse_leverage = Σ(monthly_cost over same reuse_key) × manual_share
```

### 3.2 自動化／リファクタリング実行に必要なもの

スキーマに4項目追加（YAML には**ポインタだけ**）:

1. `owner` / `approvers` — 変更権限の主体
2. `sources` — Runbook / リポジトリ / API などの参照
3. `change_risk` — blast_radius / rollback / sla
4. `evidence` — 実例・教師データ（本体は別管理）

新スキル候補: `/bizspec-automate`（候補選定 → 着手計画 → 実装ポインタ生成）

### 3.3 3年運用の現実

**そのままだと半分は考古学資料化する**。腐敗トップ3:
- サイレント・ドリフト（実プロセスは動くが YAML 据え置き）
- オーナー蒸発（異動・退職で主語不在）
- `automated` 詐称（自動化スクリプトが壊れても YAML は緑）

対策:
- `governance` フィールド（owner必須、`last_reviewed`、`review_interval_days`）+ `bizspec audit`
- `bizspec/_decisions/<unit>.md` ADR + `bizspec why <unit>`
- `automation.health_check` 必須化 + `bizspec health`（壊れたら自動降格 PR）
- `schema_version` + `migrations/` + `bizspec migrate`

**やってはいけないこと**: 承認ワークフロー / 電子署名 / RBAC を YAML に持ち込まない（Jira/GitHub の再発明）。

---

## 4. 抜本的な再設計方針 ― 3層モデル

議論を進めて辿り着いた結論：

### 4.1 BizSpec は 2 つの異なるツールを 1 スキーマに同居させていた

| 用途 | 必要情報 |
|---|---|
| **執行支援モード**（AI 実装仕様書） | aim / rule / io.in/process/out / executor.reason 全部 |
| **分析モード**（ボトルネック発見） | unit名 / 1行説明 / duration / frequency / difficulty |

100×500 を執行支援レベルまで全部書くのは現実的でない。3年も保たない。

### 4.2 3層モデル

| 層 | 形式 | 期間 | AI の役割 |
|---|---|---|---|
| **L1: 棚卸し** | スプレッドシート / CSV | **永続**（年1〜2回再棚卸） | 集計・ソート・類似検索（テキスト処理）。抽象思考はさせない |
| **L2: ホットスポット深掘り** | 自然言語の壁打ち + メモ | **作業期間限定（数週間）** | 質問差し出し・整理 |
| **L3: 本質的な問い直し** | 問いテンプレ + 会話 | 必要時起動 | 文脈に合うテンプレを呼び出す |

### 4.3 提案フロー

```
L1 (CSV) で時間食ってる unit を発見
  ↓
L2 的な深掘り（IPOっぽい質問を AI とやる、ただし YAML には書かない）
  ↓
L3 (問いテンプレ) で「そもそも要る？／自動化できる？」
  ↓
プロセス変更を提案 → L1 を更新（before/after 差分が残る）
  ↓ 
（AI エージェント化が必要になったときだけ）unit.yaml を書く
```

### 4.4 このフローの強み

**L1 CSV の対応前後 diff = 改善ポートフォリオ**

```csv
unit, description, duration, frequency, difficulty, status_before, status_after, saved_h_per_mo
月次レポート集計, ..., 2, 1, low, manual, automated, 2.0
バグ再現環境構築, ..., 1, 8, medium, manual, partially-auto, 4.0
```

3年運用すると「自分たちが何を変えてきたか」の説明可能な記録になる。**BizSpec が「プロセス改善 ROI トラッカー」になる**。経営報告にもそのまま使える。

### 4.5 認識している盲点と緩和

横断重複検出（「A でやってるから B も要らないよね」型）はこのフローでは弱い。

緩和: L2 深掘りのとき AI に L1 CSV を渡して「似た unit が他プロセスにないか」を聞く。テキスト類似と要約は AI が得意な領域。**全 unit を YAML 化しなくても、L1 の自然言語フィールドを AI に読ませれば十分**。

---

## 5. AI を使う場所の整理

> 「AI は抽象的なことを考えるのは苦手」（ユーザー観察）

| AI に向く | AI に向かない |
|---|---|
| テキスト類似検索 / 要約 | 抽象的な思考そのもの |
| 問いテンプレートを文脈に合わせて呼び出す | 「正しい問い」を独自に生み出す |
| 構造化された情報を読んで指摘する | 構造化されていない混沌から本質を見出す |
| 計測データから自動充填する | 計測されていない暗黙知を補完する |

`/bizspec-rethink`（仮）の正しい姿は **「人間が思考する瞬間に、AI が適切な問い直しテンプレートを差し出す」**。スキル本体は問いの集合体で、AI の役割は文脈に合わせてその問いを呼び出すこと。

---

## 6. ツールへの含意（重心の置き直し）

| 機能 | 重心 | 状態 |
|---|---|---|
| **L1: CSV テンプレ + 流入支援 + 改善ポートフォリオ** | ★★★ メイン | **未実装** |
| **L3: 問いテンプレスキル** | ★★ サポート | **未実装** |
| **L2 を支える AI 壁打ち** | ★★ サポート | 既存スキル（refine等）で部分対応 |
| **YAML スキーマ + refine/refactor/review** | ★ ニッチ | 実装済み（「AI 実装仕様書モード」として位置づけ直す） |
| **viz ヒートマップ等** | ★★ L1 用に再利用可 | CSV 入力対応すれば即活きる |

**既存の YAML 系資産は捨てない**（AI エージェント実装フェーズで効く）が、**メインの入口は CSV にする**。viz は CSV ベースに切り替えれば 100×500 でも普通に動く。

---

## 7. 次のステップ候補

最小手で言うと：

1. **CSV テンプレ生成**（`bizspec init` で `bizspec/portfolio.csv` を作る）
2. **CSV → viz の流入路**（YAML だけでなく CSV も読めるようにする）
3. **`/bizspec-rethink` スキル**（問いテンプレ集）
4. **L1 計測自動充填の実験**（カレンダー / CI ログから duration を埋める PoC）

これだけやれば「100×500 を最初から書かなければいけない」呪いは消える。

---

## 8. メタな学び

**「AI が魔法の杖ではない」** を出発点に進めた今日の議論で、BizSpec の正体が見えた：

- AI が肩代わりできるのは **「形式知化のラストワンマイル」** であって、何を形式化すべきかという判断そのものは人の仕事
- 構造化（YAML）は **「AI を実装層に降ろす時」だけ** 必要。改善活動そのものには重すぎる
- ツールが提供すべきは「**壁打ちが起きやすい場**」であって、構造化を強要する箱ではない
- **永続前提で作る範囲は最小化する**。L2 を流動化させると 3年運用の腐敗問題は根本解決する

`/bizspec-refine` `/bizspec-refactor` `/bizspec-review` の3スキルは「書く・直す・読む」を覆っているが、**「考える」AI**（問いテンプレを差し出す `/bizspec-rethink`）が今の最大の空白。ここを次に埋めるべき。

---

## 9. 未消化の論点

- L1 CSV の正確なフォーマット決定（前後 diff をどう表現するか）
- viz の CSV 入力対応の実装範囲
- `/bizspec-rethink` の問いテンプレートの具体化
- 既存の `bizspec/<process>/` 配下の YAML を将来どうフェードアウトさせるか（一気に削除はしない、徐々に L1 へ移行）
- スプレッドシート / Google Sheets との連携方法（CSV 経由が現実的）

---

## 10. エージェントチームによる批判的評価（同日追記）

§1〜§9 を書き終えた後、3観点（モデル整合性 / AI 能力評価 / 採用可能性）でエージェントチームに批判検証させた結果。
**結論: §1〜§9 はそのままでは設計指針に採用できない。**

### 10.1 堅い結論（採用してよい）

- **「執行支援モード」と「分析モード」を1スキーマに同居させていた** という構造的指摘（§4.1）。情報粒度・更新頻度・寿命が桁違い
- **「AI は形式知化のラストワンマイル担当、何を形式化するかは人」**（§8）の責務切り分け原則

### 10.2 揺らいでいる結論（要再考）

| 結論 | 揺らぎの正体 |
|---|---|
| 3層モデル（L1/L2/L3） | L2「壁打ち+メモ」と L3「問いテンプレ+会話」は **同じ会話の異なるモード**。層として独立させる根拠が薄い。実態は **L1 + 会話層の 2層** |
| 「AI は抽象思考が苦手」 | **2022〜2023年の認識**。Opus 4.7 級は IPO 分解・責務境界指摘・横断パターン抽出を実用レベルで実行可能。正しい原則は「AI は抽象できない」ではなく **「最終署名は人」**。両者は別物 |
| 「L2 流動化で 3年腐敗が解決」 | **L1 CSV 自体のドリフト未対処**。`status_after` や `saved_h_per_mo` の真偽は CSV でも腐る。`automated` 詐称は CSV だと health_check すら無く、YAML より腐敗が早い |
| 「95% は壁打ちで解決できる」 | **根拠なし（希望的観測）**。残り 5% の閾値判定基準も未定義 |
| 「YAML は AI エージェント実装時のみ」 | **エスカレーションパス未設計**。L2 の壁打ち中に得た中間構造（責務・rule・IO 仮説）の永続先が CSV 1行か L2 メモしかない |

### 10.3 見落としていた観点

#### 目的のすり替え疑惑

原典 `BizSpec_要求仕様.md` の主目的は「**AI-Native へのリファクタ／AI が実行可能な最小仕様化**」。
本メモは「**改善 ROI トラッカー化**」へ重心移動している。両立か乗り換えかを明示せず混ぜているのは設計判断として未整理。

#### AI 能力の過小評価が招く設計の歪み

メモの AI 能力認識を更新すると、設計判断は以下のように変わる：

- `/bizspec-review` の7軸検証は **もっと AI 主導でよい**
- `/bizspec-rethink` を **テンプレ集に閉じず**、AI が文脈駆動で問いを生成 → 人が採否する設計に拡張
- L1 CSV から `reuse_key` や横断重複を **AI が自動提案** できる（メモが「弱い」とした領域は実は AI が得意）

#### 採用可能性の現実

- **L1 CSV のメンテインセンティブが未定義**（年1〜2回再棚卸は経験上実行されない）
- **before/after 計測** は基盤がないと主観に流れ、経営報告に耐えない
- **想定ユーザー像が空欄**（10〜30人チームか、部門全体か、で機能の重さが全部ブレる）
- **Sheets + ChatGPT で代替可能**な部分が大半。BizSpec 固有価値は「unit 粒度を時系列 diff で git 履歴に乗せる」**ほぼ 1点のみ**

---

## 11. 残った最重要提言

採用可能性班の指摘がもっとも切実：

> **設計より先に、自分の実プロセス 30〜50 unit を CSV に書き起こして 3か月運用してみる「ドッグフーディング PoC」**。
> 書く動機・更新タイミング・主観精度の問題を先に観測してから、フォーマットと viz 連携を確定する。
> 「自分が3か月続けられるか」を証明してから機能を決める。

**今日の議論は全部「想像の中の 100×500」を相手にしていた**。実証なしに 3層モデルや CSV 化を実装するのは、検証されていない仮説の上に家を建てるのと同じ。

---

## 12. 次に取るべき行動（修正版）

§7 で示した次ステップは保留し、以下の順に再構成する。

1. **目的の再定義**: BizSpec は「AI-Native 仕様化ツール」か「改善 ROI トラッカー」か、両者を明示的に両立させるのか、を判断する
2. **想定ユーザーペルソナの明文化**: 誰が・何人で・どの粒度で使うか
3. **ドッグフーディング PoC**: 自分の実プロセス 30〜50 unit を CSV（または既存 YAML 簡易版）で書き起こし、3か月メンテし続けられるか実証
4. **PoC 結果に基づく機能決定**: 上記が回ってから、L1 / 会話層 / YAML エスカレーションの具体形を決める
5. **AI 能力認識の更新**: 「テキスト類似と要約だけ」と AI を縛らず、抽象思考の初稿提案者としても活用する設計を検討する

これらが終わってから §6（ツールへの含意）と §7（次のステップ候補）を書き直す。

---

## 13. 現時点の進捗（同日追記）

§12 のうち、**ドッグフーディング PoC の入口として CSV テンプレートのみ作成** した状態。

- ファイル: `bizspec/portfolio.csv`
- 列: `No / プロセス名 / ユニット名 / 説明 / duration / frequency / difficulty`
- 中身: ヘッダ + 値の形式説明1行 + サンプル1行のみ

**意図的にやっていないこと:**
- L1 → viz 連携の実装
- `/bizspec-rethink` スキルの新設
- AI 自動充填の PoC
- 既存 YAML からの移行

設計判断（§12 の手順1〜2）が済んでいないので、機能側は手をつけない。
まずはこの CSV を自分の実プロセスで埋めて 3か月運用できるかを観察する段階。

---

## 14. 形式とインターフェースの再整理（同日追記）

スター・コントリビュータがゼロのうちに方向修正できる前提で進めた追加議論。

### 14.1 原典との関係の読み直し

`BizSpec_要求仕様.md` v1 は実は「全 unit を YAML 化せよ」とは書いていない。記述されているのは「AI 実行可能な最小仕様の形式」と「価値提供に直結する作業を抽出」だけで、**掘る範囲の優先順位付けは未定義**のまま放置されていた。

「全プロセス YAML 化」は原典にない過剰解釈で、**L1（CSV 棚卸し）でレバレッジが立つ unit を選別 → そこだけ YAML 化** という二段構えは、原典の暗黙の前提を明示化した形と読める。すり替えではない。

### 14.2 インターフェースモデル

CSV を BizSpec のインターフェースとし、ユーザーは好きな表計算ツールにインポート → 編集 → CSV でエクスポート、で戻す。試作 `bizspec/portfolio.csv` の情報量なら入力コストは YAML より圧倒的に低い。

### 14.3 形式の正解は決めない

CSV の本質的弱点（プロセス列冗長、エスケープ事故、型なし）は認識済み。ただし §11 でフォーマット先行は禁じ手と確認済みなので、**PoC を走らせて辛くなった瞬間に Markdown table / YAML フラット / TSV に乗り換える**方針。乗り換えコストは数十行のスクリプトで済む。

### 14.4 二重定義への対応 ― CSV を背骨、それ以外は衛星

| 層 | 持つ情報 |
|---|---|
| CSV（背骨） | unit名 / 説明 / duration / frequency / difficulty / 拡張列 |
| YAML（衛星・任意） | aim / rule / link / core / io / executor のみ |
| メモ等（衛星・任意） | 深掘りログ / ADR / リンク集 |

ルール：
- unit 名や duration を YAML 側に書くのは責務違反（二重定義の発生源）
- viz・list は CSV と衛星を join して表示
- 結合キーは **unit 名**（slug/ID は導入しない、リネーム時は CLI で衛星追従）
- 孤立衛星（CSV に存在しない unit を指す）は warn 止まり、削除はしない

---

## 15. バグ修正自動化フローでの実証イメージ（同日追記）

最初の dogfooding 候補として **バグ修正の自動化（有人レビューあり）**。想定フロー：

```
1. CSV を埋める
2. バグ修正に関係あるプロセスにチェックを付ける
3. IPO や作業内容を定義
4. 自動化可能性 + 「そもそも必要か？」の精査
5. 自動化プロセスを設計
6. 試運転
7. 問題あれば修正、行けそうなら合意取り → プロセス・ルール変更
```

### 15.1 暗黙に必要になる CSV 拡張列

| 列 | 用途 |
|---|---|
| タグ | `bug-fix` 等で関係プロセスをフィルタ |
| 自動化ステータス | `未着手 / 検討中 / 試運転中 / 完了` |
| 必要性判定 | `必要 / 不要 / 保留`（「そもそも必要か？」の答え） |

**PoC 走行前に列を確定しない**。走らせながら不足が出たら足す。

### 15.2 IPO・作業内容の置き場所が形式分岐点

- **検討中段階・人間が読むだけ** → CSV か Markdown 自由記述（軽い）
- **AI 実行に降ろす段階・試運転以降** → YAML（重いが構造化）

同じ unit でも **改善 PoC のステージ進行に伴い CSV → YAML へ昇格** する。これがエスカレーションパス。

### 15.3 発見：メタプロセスとしての「スプリント開始時のトリガー精査」

自動化が走り始めると **「今スプリントでどの自動化を起動するか」を判断するプロセス**が新しく生まれ、これ自体 BizSpec の管理対象 unit になる。

含意：BizSpec の管理対象は「業務の棚卸し」だけでなく **「改善活動そのもの」** にも広がる。原典の想定範囲を超える発見。

### 15.4 BizSpec の正体の再定義

```
棚卸し（L1）→ 深掘り（L2）→ 自動化設計（YAML 化）
  → 試運転 → 合意取り（before/after 資料）→ プロセス反映
```

BizSpec は単なる棚卸しツールではなく **改善 PoC ライフサイクル管理ツール** に近づいている。CSV の拡張列はこのパイプラインのステージマーカーとして機能する。

### 15.5 早期に試す価値のある機能

「合意取り」が PoC 成功の最大ボトルネック候補。`bizspec list --diff before-after` 相当でチームへの説明資料を生成できるかは早めに観察したい。ただしこれもフローを実走させてから判断する。

